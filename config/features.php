<?php

return [

    /*
    |--------------------------------------------------------------------------
    | فهرستِ پرچم‌های قابلیت
    |--------------------------------------------------------------------------
    |
    | ⚠️ این فهرست **صریح** است و نه «هر کلیدی که کسی بنویسد». دلیلش:
    |
    | ① پرچمی که در این فهرست نباشد، از مسیرِ API قابلِ روشن‌کردن نیست. بدونِ
    |    آن، هر کسی که به پنلِ سوپرادمین دسترسی دارد می‌توانست کلیدِ دلخواه
    |    بسازد و جدولِ `settings` پر از کلیدِ مرده می‌شد.
    | ② پرچمِ فراموش‌شده بدترین نوعِ بدهیِ فنی است: کدِ هر دو مسیر برای همیشه
    |    می‌ماند چون کسی مطمئن نیست خاموشش امن است. وقتی همه‌ی پرچم‌ها یک‌جا
    |    نوشته شده‌اند، مرورشان ممکن است.
    |
    | `default` مقداری است که وقتی هیچ‌کس دستش نزده اعمال می‌شود.
    |
    | ⚠️ مقدارِ واقعی از **دیتابیس** خوانده می‌شود، نه از `.env`. تغییرِ `.env`
    | نیازمندِ استقرارِ دوباره و `config:cache` است؛ خواسته‌ی این مرحله دقیقاً
    | روشن/خاموش‌کردن **بدونِ استقرارِ دوباره** بود.
    |
    */

    'flags' => [

        'messenger' => [
            'label' => 'پیام‌رسانِ داخلی',
            'description' => 'گفت‌وگوی ساکنین و مدیر در پنل. خاموش‌کردنش دسترسیِ کاربر را می‌بندد ولی پیام‌های قبلی پاک نمی‌شود.',
            'default' => true,
        ],

        'polls' => [
            'label' => 'نظرسنجی',
            'description' => 'ساخت و رأی‌دادن به نظرسنجی‌های مجتمع.',
            'default' => true,
        ],

        'online_payment' => [
            'label' => 'پرداختِ آنلاین',
            'description' => 'درگاهِ بانکی. ⚠️ خاموش‌کردنش راهِ نجات است وقتی درگاه قطع است: کاربر به‌جای خطای بانک، گزینه‌ی رسیدِ دستی می‌بیند.',
            'default' => true,
        ],

        'support_bot' => [
            'label' => 'دستیارِ پشتیبانی',
            'description' => 'پاسخ‌گویِ خودکارِ صفحه‌ی پشتیبانی.',
            'default' => true,
        ],

        'public_registration' => [
            'label' => 'ثبت‌نامِ عمومی',
            'description' => '⚠️ خاموش‌کردنش جلوی ساختِ حسابِ تازه را می‌گیرد. برای وقتی که ثبت‌نامِ خودکار مورد سوءاستفاده قرار گرفته باشد.',
            'default' => true,
        ],
    ],

];
