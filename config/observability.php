<?php

/*
|--------------------------------------------------------------------------
| پایش و تحلیل (Observability)
|--------------------------------------------------------------------------
|
| این فایل فقط **پیش‌فرض‌ها** را از `.env` می‌خواند. مقدارِ نهاییِ هر کلید را
| `App\Support\Observability` تعیین می‌کند و اولویت این است:
|
|     تنظیماتِ پنلِ ادمین  ⟶  .env  ⟶  خاموش
|
| یعنی صاحبِ پروژه دو راه دارد و هیچ‌کدام نیاز به تغییرِ کد ندارد:
|   • برای استقرارِ سروری: مقادیر را در `.env` بگذارد
|   • برای تغییرِ سریع بدون دسترسیِ SSH: از «پایش و تحلیل» در پنلِ ادمینِ کل
|
| اگر مقداری تنظیم نشده باشد، آن سرویس **کاملاً خاموش** است: نه اسکریپتی در
| صفحه تزریق می‌شود و نه چانکی دانلود. پس نبودنِ شناسه هیچ هزینه‌ای ندارد.
|
*/

return [

    /*
    |--------------------------------------------------------------------------
    | Sentry — رهگیریِ خطا
    |--------------------------------------------------------------------------
    |
    | `dsn` هم برای بک‌اند و هم فرانت استفاده می‌شود مگر اینکه `client_dsn`
    | جدا داده شود (بعضی تیم‌ها پروژه‌ی جدا برای فرانت می‌سازند).
    |
    | `traces_sample_rate` نرخِ نمونه‌برداریِ کارایی است. صفر یعنی فقط خطاها؛
    | روی سایتِ پرترافیک عددِ بزرگ هم هزینه‌ی سهمیه دارد و هم کندی.
    */
    'sentry' => [
        'dsn' => env('SENTRY_DSN'),
        'client_dsn' => env('SENTRY_CLIENT_DSN'),
        'environment' => env('SENTRY_ENVIRONMENT', env('APP_ENV', 'production')),
        'traces_sample_rate' => (float) env('SENTRY_TRACES_SAMPLE_RATE', 0.0),
        /** فقط برای آپلودِ source map هنگام بیلد؛ هرگز به مرورگر نمی‌رود. */
        'auth_token' => env('SENTRY_AUTH_TOKEN'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Google Analytics 4
    |--------------------------------------------------------------------------
    |
    | `measurement_id` شناسه‌ی عمومی است (`G-XXXXXXXXXX`) و در مرورگر دیده
    | می‌شود. `api_secret` فقط برای Measurement Protocol (رویدادِ سمتِ سرور)
    | لازم است و **محرمانه** است.
    */
    'ga4' => [
        'measurement_id' => env('GA4_MEASUREMENT_ID'),
        'api_secret' => env('GA4_API_SECRET'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Google Tag Manager
    |--------------------------------------------------------------------------
    |
    | اگر GTM فعال باشد معمولاً GA4 را هم خودش مدیریت می‌کند. هر دو را
    | هم‌زمان روشن نکنید مگر آگاهانه، وگرنه هر بازدید دوبار شمرده می‌شود.
    */
    'gtm' => [
        'container_id' => env('GTM_CONTAINER_ID'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Microsoft Clarity — ضبطِ جلسه و نقشه‌ی حرارتی
    |--------------------------------------------------------------------------
    */
    'clarity' => [
        'project_id' => env('CLARITY_PROJECT_ID'),
    ],

    /*
    |--------------------------------------------------------------------------
    | ثبتِ خطا در دیتابیسِ خودمان
    |--------------------------------------------------------------------------
    |
    | مستقل از سرویس‌های بیرونی کار می‌کند، پس پنلِ ادمین حتی بدونِ Sentry هم
    | داده‌ی واقعی برای نمایش دارد. `retention_days` جلوی بی‌نهایت بزرگ‌شدنِ
    | جدول را می‌گیرد.
    */
    'error_log' => [
        'enabled' => (bool) env('ERROR_LOG_ENABLED', true),
        'retention_days' => (int) env('ERROR_LOG_RETENTION_DAYS', 30),
        /** سقفِ خطاهای ثبت‌شده از یک IP در دقیقه، تا گزارشِ کلاینت سیل نشود. */
        'client_rate_limit' => (int) env('ERROR_LOG_CLIENT_RATE_LIMIT', 20),
    ],

];
