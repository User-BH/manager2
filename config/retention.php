<?php

return [

    /*
    |--------------------------------------------------------------------------
    | سیاستِ نگه‌داریِ داده (R45)
    |--------------------------------------------------------------------------
    |
    | ⚠️ چرا یک فایل و نه پنج دستورِ جدا: پاک‌کردنِ داده‌ی کاربر برگشت‌ناپذیر
    | است. وقتی همه‌ی قاعده‌ها یک‌جا نوشته شده‌اند، می‌شود در یک نگاه مرورشان
    | کرد و پرسید «آیا واقعاً می‌خواهیم این را پاک کنیم؟». پنج دستورِ پراکنده
    | یعنی هیچ‌کس هرگز کلِ سیاست را نمی‌بیند.
    |
    | هر قاعده:
    |   table     جدولِ هدف
    |   column    ستونِ زمانی که سنجیده می‌شود
    |   days      از این تعداد روز قدیمی‌تر پاک می‌شود
    |   where     شرطِ اضافه (اختیاری) — «فقط چیزی که واقعاً مرده است»
    |   label     توضیحِ فارسی برای خروجی
    |
    */

    'rules' => [

        /*
         * ⚠️ بدترین موردِ اندازه‌گیری‌شده.
         *
         * هر تلاشِ ورود، هر ثبت‌نام و هر بازیابیِ رمز یک ردیف می‌سازد و
         * **هیچ‌وقت پاک نمی‌شد**. کدی که چند دقیقه پس از ساخت منقضی شده،
         * تا ابد روی دیسک می‌ماند. در مقیاسِ یک‌میلیون کاربر این پرحجم‌ترین
         * جدولِ سامانه می‌شود، بی‌آنکه یک ردیفش به درد بخورد.
         *
         * هفت روز عمداً بیشتر از انقضای کد است: در بررسیِ سوءاستفاده
         * («چند بار برای این شماره کد فرستاده شده؟») همین تاریخچه لازم است.
         */
        [
            'table' => 'otp_codes',
            'column' => 'created_at',
            'days' => 7,
            'label' => 'کدهای یک‌بارمصرفِ منقضی',
        ],

        /*
         * ⚠️ لاراول نشستِ دیتابیسی را خودش پاک نمی‌کند.
         *
         * `SESSION_LIFETIME` فقط می‌گوید نشست کِی نامعتبر است، نه اینکه
         * ردیفش پاک شود. یعنی هر ورودِ هر کاربر یک ردیفِ ماندگار است و
         * جدولی که در هر درخواست خوانده و نوشته می‌شود، بی‌مرز بزرگ می‌شود.
         *
         * ستون `last_activity` مُهرِ یونیکس است، نه تاریخ — پس این قاعده
         * پرچمِ خودش را دارد.
         */
        [
            'table' => 'sessions',
            'column' => 'last_activity',
            'unix' => true,
            'days' => 30,
            'label' => 'نشست‌های رهاشده',
        ],

        /*
         * ⚠️ فقط اعلانِ **خوانده‌شده** پاک می‌شود.
         *
         * اعلانِ نخوانده هنوز پیامی است که کاربر ندیده؛ پاک‌کردنش یعنی
         * خبری که مثلاً «قبضِ شما سررسید شده» بی‌آنکه کسی ببیندش ناپدید
         * شود.
         */
        [
            'table' => 'notifications',
            'column' => 'created_at',
            'days' => 90,
            'where' => ['read_at', 'not null'],
            'label' => 'اعلان‌های خوانده‌شده',
        ],

        /*
         * سیاهه‌ی ممیزی عمداً بلندترین بازه را دارد.
         *
         * ⚠️ یک سال، نه سه ماه: این‌ها ردِ «چه کسی چه چیزی را حذف کرد»
         * هستند و دقیقاً وقتی لازم می‌شوند که اختلافی پیش آمده باشد — که
         * معمولاً ماه‌ها بعد است.
         */
        [
            'table' => 'audit_logs',
            'column' => 'created_at',
            'days' => 365,
            'label' => 'سیاهه‌ی ممیزی',
        ],

        /*
         * ⚠️ این کلیدِ تنظیمات از قبل وجود داشت و **هیچ‌جا اعمال نمی‌شد**.
         *
         * `ERROR_LOG_RETENTION_DAYS=30` در `config/observability.php`
         * تعریف شده بود ولی هیچ کدی نمی‌خواندش — دقیقاً همان الگوی
         * `FILESYSTEM_DISK` که آن هم مرده بود.
         *
         * خودِ جدول `fingerprint` یکتا دارد، پس با تکرارِ یک خطا بزرگ
         * نمی‌شود؛ ولی خطاهای متمایزِ یک سالِ گذشته هم چیزی نیستند که کسی
         * نگاهشان کند.
         */
        [
            'table' => 'error_events',
            'column' => 'created_at',
            'days' => (int) env('ERROR_LOG_RETENTION_DAYS', 30),
            'label' => 'رویدادهای خطا',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | اندازه‌ی هر تکه
    |--------------------------------------------------------------------------
    |
    | ⚠️ این عدد در مقیاس، فرقِ بینِ نگه‌داری و قطعی است.
    |
    | یک `DELETE` بدونِ سقف روی جدولی با ده‌ها میلیون ردیف، تراکنشی می‌سازد
    | که دقایق طول می‌کشد و در تمامِ آن مدت ردیف‌ها قفل‌اند — یعنی همان
    | دستوری که برای سلامتِ سامانه نوشته شده، خودش سامانه را می‌خواباند.
    |
    | با تکه‌های کوچک، هر تراکنش میلی‌ثانیه‌ای است و بینِ تکه‌ها فضا برای
    | درخواست‌های واقعی باز می‌ماند.
    |
    */

    'chunk' => (int) env('RETENTION_CHUNK', 1000),

];
