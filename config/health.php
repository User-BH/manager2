<?php

return [

    /*
    |--------------------------------------------------------------------------
    | رمزِ گزارشِ کاملِ سلامت
    |--------------------------------------------------------------------------
    |
    | مسیرِ `/up` بدونِ احراز هویت باز است تا متعادل‌کننده‌ی بار و اسکریپتِ
    | استقرار بتوانند بزنندش. بدونِ این رمز فقط `{"status": "..."}` برمی‌گردد؛
    | با آن (در هدرِ `X-Health-Secret`) گزارشِ کاملِ شش سنجه.
    |
    | خالی‌گذاشتنش امن است — یعنی گزارشِ کامل از بیرون در دسترس نیست.
    |
    */

    'secret' => env('HEALTH_SECRET'),

    /*
    |--------------------------------------------------------------------------
    | هشدار
    |--------------------------------------------------------------------------
    |
    | ⚠️ این پروژه عمداً هیچ کانالِ خروجیِ همگانی ندارد: ایمیل کلاً کنار
    | گذاشته شده و پیامک فقط برای کدِ یک‌بارمصرف است. پس هشدار به‌صورتِ
    | پیش‌فرض در کانالِ لاگِ `alerts` می‌نشیند و در پنلِ سوپرادمین دیده
    | می‌شود.
    |
    | اگر جایی برای فرستادنِ هشدار دارید (هر سرویسی که وب‌هوک قبول کند)،
    | آدرسش را در `ALERT_WEBHOOK_URL` بگذارید. خالی باشد، چیزی فرستاده
    | نمی‌شود و بقیه‌ی مسیرها سرِ جایشان‌اند.
    |
    */

    'alert' => [

        'webhook' => env('ALERT_WEBHOOK_URL'),

        /*
        | ⚠️ بدونِ این، یک دیسکِ پر با اجرای هر پنج دقیقه‌ای، روزی ۲۸۸ هشدارِ
        | یکسان می‌سازد — و صفی که پر از هشدارِ تکراری است، همان‌قدر بی‌فایده
        | است که هشدار نداشته باشی. هر مشکل تا این مدت فقط یک بار اعلام
        | می‌شود.
        */
        'throttle_minutes' => (int) env('ALERT_THROTTLE_MINUTES', 60),
    ],

];
