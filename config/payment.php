<?php

return [

    /*
    |--------------------------------------------------------------------------
    | درگاه ساختگی (سندباکس)
    |--------------------------------------------------------------------------
    |
    | `FakeGateway` هر بازگشتی را تایید می‌کند و کد رهگیری جعلی می‌سازد. روی
    | سرور واقعی یعنی هر «پرداخت آنلاین» بدون جابه‌جایی یک ریال، قبض را
    | تسویه‌شده علامت می‌زند — یعنی شارژ رایگان برای هر ساکنی که این را بفهمد.
    |
    | پس به‌صورت پیش‌فرض فقط بیرون از محیط production روشن است. اگر سرور
    | نمایشی (staging/demo) دارید که عمداً باید پرداخت ساختگی داشته باشد،
    | صراحتاً در .env روشنش کنید:
    |
    |     PAYMENT_SANDBOX_ENABLED=true
    |
    | این کلید هم درگاه شارژِ مجتمع‌ها را پوشش می‌دهد و هم درگاه اشتراک را.
    |
    */

    'sandbox_enabled' => (bool) env(
        'PAYMENT_SANDBOX_ENABLED',
        env('APP_ENV', 'production') !== 'production',
    ),

];
