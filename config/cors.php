<?php

return [

    /*
    |--------------------------------------------------------------------------
    | CORS
    |--------------------------------------------------------------------------
    |
    | این فایل تا R16 وجود نداشت و لاراول از پیش‌فرضِ خودش استفاده می‌کرد:
    | `allowed_origins => ['*']`.
    |
    | آن پیش‌فرض «رخنه» نبود — چون `supports_credentials` خاموش است، مرورگر
    | کوکیِ نشست را به درخواستِ بین‌دامنه‌ای **نمی‌چسباند** و سایتِ مهاجم
    | نمی‌تواند داده‌ی کاربرِ واردشده را بخواند. ولی دسترسی‌ای می‌داد که
    | هیچ‌کس به آن نیاز ندارد: رابطِ کاربری از **همان دامنه** سرو می‌شود، پس
    | این سامانه اصلاً مصرف‌کننده‌ی بین‌دامنه‌ای ندارد.
    |
    | پس پیش‌فرض اینجا «هیچ دامنه‌ای» است. اگر روزی اپِ موبایل یا رابطی روی
    | دامنه‌ی دیگر اضافه شد، فقط همان دامنه در `.env` نوشته می‌شود — نه `*`.
    |
    */

    'paths' => ['api/*', 'sanctum/csrf-cookie'],

    'allowed_methods' => ['GET', 'POST', 'PUT', 'PATCH', 'DELETE', 'OPTIONS'],

    /*
     * فهرستِ جداشده با ویرگول، مثلاً: CORS_ALLOWED_ORIGINS=https://app.example.com
     *
     * خالی یعنی هیچ درخواستِ بین‌دامنه‌ای مجاز نیست. درخواست‌های هم‌دامنه
     * اصلاً درگیرِ CORS نمی‌شوند، پس این تنظیم رابطِ فعلی را لمس نمی‌کند.
     */
    'allowed_origins' => array_values(array_filter(
        array_map('trim', explode(',', (string) env('CORS_ALLOWED_ORIGINS', ''))),
    )),

    'allowed_origins_patterns' => [],

    'allowed_headers' => ['Accept', 'Content-Type', 'X-Requested-With', 'X-XSRF-TOKEN', 'Origin'],

    'exposed_headers' => [],

    // پاسخِ preflight یک ساعت کش شود تا هر درخواست دوبار به سرور نخورد
    'max_age' => 3600,

    /*
     * ⚠️ روشن‌کردنِ این گزینه بدونِ فهرستِ دقیقِ دامنه‌ها خطرناک است: یعنی
     * سایتِ دیگری می‌تواند با کوکیِ کاربر به API ما درخواست بزند و پاسخ را
     * بخواند. احراز هویت اینجا مبتنی بر کوکیِ هم‌دامنه است و به این نیاز ندارد.
     */
    'supports_credentials' => false,

];
