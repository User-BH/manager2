# حافظه‌ی پایدار پروژه — ساکنا

> **این فایل را در ابتدای هر جلسه بخوان و در پایان هر کار به‌روزرسانی کن.**
> به حافظه‌ی داخلی مدل تکیه نکن؛ منبعِ درستی همین فایل + گیت است.
>
> 📋 **برنامه‌ی اجراییِ فعال: [`docs/WORK_PLAN.md`](WORK_PLAN.md)** — مراحل `R1..R48` بر پایه‌ی فایل «ایرادات ساکنا».
> کاربر با گفتن «انجام R<شماره>» مرحله را سفارش می‌دهد. پاسخِ هر مرحله باید قالبِ ۴ پرسشیِ انتهای همان سند را داشته باشد
> (مشکل چه بود؟ / چرا مشکل است؟ / چه تغییری شد؟ / چه تأثیری دارد؟).

- **آخرین کامیت بررسی‌شده:** `3780e7c` — «OTP-required registration, member delete/deferred search, real Bale+Rubika icons»
- **شاخه:** `main` · **تعداد کامیت:** ۶۴ · **وضعیت working tree:** تمیز
- **تاریخ ممیزی:** ۱۴۰۵/۰۵/۰۴ (2026-07-26)
- **مخزن:** https://github.com/User-BH/manager2

---

## ۱) معماری و تکنولوژی‌ها

| لایه        | انتخاب                                              | نکته                                                      |
| ----------- | --------------------------------------------------- | --------------------------------------------------------- |
| بک‌اند      | Laravel 13 / PHP 8.4.23                             | API محور (JSON)، بدون Blade برای پنل                      |
| دیتابیس     | MySQL/MariaDB (پروداکشن)، SQLite (تست/دِو)          | ۳۰ مهاجرت، همه اجرا شده                                   |
| فرانت       | React 19.2 + TypeScript + Tailwind v4               | ۷۳ صفحه، ۲۵ کامپوننت مشترک                                |
| بیلد        | Vite 8 (rolldown) + React Compiler                  | `manualChunks` باید **تابع** باشد نه آبجکت                |
| روتینگ      | **هیبرید**: MPA برای صفحات عمومی + SPA برای داشبورد | مهم — پایین توضیح داده شده                                |
| حالت سراسری | **zustand** (۵ store)                               | فایل‌ها هنوز در پوشه‌ی `context/` هستند (نام‌گذاری قدیمی) |
| فرم         | React Hook Form + Zod                               | `Controller` برای فیلدهای محدودشده                        |
| HTTP        | axios (پشت `lib/api.ts`)                            | مدیریت CSRF + بازتلاش ۴۱۹                                 |
| تاریخ       | morilog/jalali + `App\Support\Jalali`               | کل UI شمسی                                                |
| پیامک       | درایور تعویض‌پذیر؛ فعال: **IPPanel Edge API**       | فقط برای OTP                                              |

### معماری روتینگ (تصمیم کلیدی)

- صفحات عمومی (`/`, `/demo`, `/support`, `/auth`) **MPA جزیره‌ای**‌اند: هر کدام یک روتِ لاراول + یک سند HTML مستقل با `<head>` سرورساید (برای SEO)، و یک entry ری‌اکت جدا که فقط همان صفحه را mount می‌کند (`resources/js/entries/*.tsx`).
- داشبورد یک **SPA** کامل با react-router است (`resources/js/main.tsx` → `AppRouter`).
- **پیامد مهم:** ناوبری بین این دو دنیا باید **بارگذاری کامل صفحه** باشد. `navigate('/')` سمت‌کلاینت از داشبورد به خانه کار نمی‌کند (باگ ۴۰۳ که در `3b7987e` رفع شد). برای این کار `lib/mpaNav.ts` وجود دارد.

### چندمستأجری (Multi-tenancy) — حساس‌ترین بخش

سه لایه‌ی مستقل:

1. `ComplexScope` (global scope) روی **۱۳ مدل** از طریق trait `BelongsToComplex`.
2. `resolveRouteBinding()` در همان trait → جلوگیری از IDOR بین مجتمع‌ها هنگام route-model binding.
3. `TenantContext` (singleton) + `ComplexResolver` + میدل‌ور `SetCurrentComplex`.

**مدل‌های بدون scope (عمدی، سراسری):** `User`, `Complex`, `Plan`, `Setting`, `OtpCode`, `TrustedDevice`, `AuditLog`, `Backup`, `AnnouncementRead`.
⚠️ چون `User` scope ندارد، هر کوئریِ کاربر **باید دستی** با `complex_id` فیلتر شود (الگوی موجود در `ResidentController` درست است).

### ترتیب میدل‌ورها (حساس)

`StartSession(3)` → `AuthenticateTrustedDevice(5)` → `AuthenticatesRequests(6)` → `EnsureActive` → `SetCurrentComplex` → `SubstituteBindings`

> در `bootstrap/app.php` با `prependToPriorityList` تنظیم شده. حتماً باید **اینترفیس** `Illuminate\Contracts\Auth\Middleware\AuthenticatesRequests` هدف قرار گیرد، نه کلاس `Authenticate`.

---

## ۲) قابلیت‌های تکمیل‌شده

- **احراز هویت:** ورود دومرحله‌ای (رمز → OTP پیامکی)، دستگاه مورداعتماد ۱۰ روزه (کوکی امضاشده و خارج از `EncryptCookies`)، بازیابی رمز، پازل کپچا، «مرا به خاطر بسپار».
- **ثبت‌نام دومرحله‌ای:** داده در نشست می‌ماند و **حساب فقط پس از تایید کد ساخته می‌شود** (ثبت‌نام نیمه‌کاره هیچ رکوردی باقی نمی‌گذارد).
- **مالی:** موتور شارژ با ۸ روش، صدور قبض دوره‌ای، جریمه/تخفیف، پرداخت آنلاین (درگاه) + آپلود رسید با گردش تایید.
- **اشتراک:** پکیج‌های **پویا از دیتابیس** (`plans`) که ادمین کل تعریف می‌کند + فعال‌سازی/غیرفعال‌سازی دستی برای هر مجتمع.
- **مدیریت:** واحدها، ساکنین، مدیران، اطلاعیه، پیام‌رسان، گزارش مالی، بکاپ دو سطحی، لاگ فعالیت.
- **ادمین کل:** مجتمع‌ها، اعضای سامانه (جست‌وجو/تغییر نقش/حذف)، تبلیغات، پکیج‌ها، فوتر و شبکه‌های اجتماعی، پنل پیامک.
- **عمومی:** صفحه‌ی فرود، دمو، پشتیبانی با چت‌بات محلی فارسی، SEO کامل (OG/JSON-LD/sitemap/robots/llms.txt).

---

## ۳) تصمیم‌های مهم (و دلیلشان)

| تصمیم                                                      | دلیل                                                                     |
| ---------------------------------------------------------- | ------------------------------------------------------------------------ |
| MPA برای صفحات عمومی                                       | SEO واقعی + HTML سرورساید برای خزنده‌ها و هوش مصنوعی‌ها                  |
| zustand به‌جای Context                                     | حذف Provider‌های تودرتو؛ **Redux عمداً نصب نشده**                        |
| پکیج‌ها در دیتابیس + `plan_id` اختیاری روی `subscriptions` | افزایشی و بدون شکستن پرداخت‌های قبلی؛ enum قدیمی به‌عنوان fallback مانده |
| پیامک فقط برای OTP                                         | یادآوری شارژ پیامکی عمداً حذف شد (`53e3ce0`)                             |
| بدون وابستگی خارجی در UI                                   | فونت/CSS/JS همه self-host (شرایط ایران)                                  |
| چت پشتیبانی محلی (بدون LLM)                                | قید «بدون وابستگی خارجی»                                                 |
| `SubscriptionPlan::Free`                                   | هنوز به‌عنوان fallbackِ «بدون اشتراک» زنده است                           |

---

## ۴) اصلاحات امنیتی انجام‌شده (تاریخچه)

| کامیت     | موضوع                                                         |
| --------- | ------------------------------------------------------------- |
| `a4b5944` | بستن نشت بین‌مستأجری روی هر روتی که مدل را با id بایند می‌کند |
| `a6c62b1` | scope کردن ارجاعات بین‌مستأجری                                |
| `bd38390` | محدودیت نرخ روی احراز هویت + اعمال واقعی پلن‌ها               |
| `567736d` | غیرفعال‌سازی درگاه سندباکس روی سرور پروداکشن                  |
| `b4dcd86` | idempotent و مستقل‌از‌نشست کردن callback درگاه                |
| `4d0d210` | محافظت از بازیابی بکاپ سیستم                                  |
| `36e6eb6` | مخفی‌سازی واقعی پیام + پاسخ JSON برای حساب غیرفعال            |
| `32dd884` | پنجره‌ی پیام‌رسان، نگه‌داری، محدودیت نرخ، ردگیری (audit)      |
| `8328d2e` | ورود دومرحله‌ای + دستگاه مورداعتماد                           |

---

## ۵) دستورهای اجرا و تست (نتیجه‌ی واقعی — 2026-07-26)

```bash
php artisan test        # ✅ 277 passed / 997 assertions
npm test                # ✅ 43 passed (۵ فایل) — Vitest
npm run typecheck       # ✅ exit 0، بدون خطا
npm run lint            # ✅ exit 0 — oxlint، ۰ خطا
npm run check           # هر سه با هم (typecheck + lint + test)
npm run build           # ✅ موفق (~1s)
php artisan migrate:status   # ✅ همه‌ی ۳۰ مهاجرت Ran
php artisan route:list       # ✅ ۱۳۲ سطر
npm audit --omit=dev    # ⚠️ ۱ advisory (react-router، غیرقابل‌اعمال — پایین ببین)
```

**قابل اجرا نیست (دلیل دقیق):**

- **`composer audit`:** فرمان `composer` روی PATH این محیط نیست (تست‌ها مستقیم با `php artisan` اجرا شدند) ⇒ آسیب‌پذیری وابستگی‌های PHP بررسی‌نشده باقی مانده.

**اسکریپت‌ها:** `package.json` شامل `dev`, `build`, `test`, `test:watch`, `lint`, `typecheck`, `check`. `composer.json` دارای `setup`, `dev`, `test`.

---

## ۶) محدودیت‌های شناخته‌شده

- **پوشش تست فرانت: پایه‌ای.** ۴۳ تست روی منطقِ بحرانی (پالایه‌ها، طرح‌های اعتبارسنجی، ماندگاری ورود، خطای API، محافظت مسیر). **کامپوننت‌های تعاملی هنوز تست تعاملی ندارند** (R28).
- **لینتر oxlint است نه ESLint** — چون `typescript-eslint` هنوز TypeScript 7 را پشتیبانی نمی‌کند و هنگام اجرا صریحاً خطا می‌دهد ([#10940](https://github.com/typescript-eslint/typescript-eslint/issues/10940)). با انتشار نسخه‌ی سازگار می‌توان مهاجرت کرد (R27).
- **آسیب‌پذیری react-router:** یک advisory باز ([GHSA-qwww-vcr4-c8h2](https://github.com/advisories/GHSA-qwww-vcr4-c8h2)) که **در این پروژه قابل بهره‌برداری نیست** (مختص حالت RSC؛ ما فقط `BrowserRouter` داریم). نسخه‌ی ۸ هنوز منتشر نشده و **برگشت به ۷٫۱۱٫۰ وضعیت را بدتر می‌کند** (۱۴ advisory به‌جای ۱). تصمیم: ماندن روی ۷٫۱۸٫۱ تا انتشار ۸٫۳٫۰. جزئیات کامل: `REMAINING_WORK.md#R1`.
- `useTransition`/`useDeferredValue` فقط در **یک فایل** (`MembersPage.tsx`) استفاده شده.
- `config/landing.php` دیگر **هیچ‌جا استفاده نمی‌شود** (تبلیغات به دیتابیس منتقل شد) و یک TODO کهنه دارد.
- صف (queue) پیکربندی شده (`QUEUE_CONNECTION=database`) اما **هیچ Job‌ای وجود ندارد**.
- هیچ Policy/Gate‌ای وجود ندارد؛ کل مجوزدهی با میدل‌ور `role:` + `abort_unless/abort_if` پراکنده در ۱۵ کنترلر است.
- ویدیوی دمو (`public/videos/demo.mp4`) و تصاویر گالریِ ایرانی هنوز از سمت کارفرما تحویل نشده.
- شخصیت «بازدیدکننده» و دموی فقط‌خواندنی هنوز ساخته نشده (نیازمندی محصولی باز).

### داده‌ی محیط توسعه

کاربران نمونه: `09120000001` (ادمین کل)، `09120000000` (مدیر مجتمع)، `0912111110x` (مالک)، `0912222220x` (مستاجر).

> در جریان ممیزی رمز `09120000000` و `09122222202` روی `audit1234` تنظیم شد و `otp_disabled` پس از تست به `false` بازگردانده شد.
