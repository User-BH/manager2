# کارهای انجام‌شده — ساکنا

استخراج‌شده از تاریخچه‌ی گیت (۶۴ کامیت، تا `3780e7c`) و راستی‌آزمایی‌شده با اجرای واقعی تست‌ها در 2026-07-26.

**ریسک رگرسیون** بر این مبنا تعیین شده: پوششِ تست موجود × حساسیت دامنه (پول/دسترسی/داده) × میزان تماس با بقیه‌ی سیستم.

---

## ۱) زیرساخت استقرار و راه‌اندازی

- **کامیت‌ها:** `2178310`, `886ea50`, `cac9c88`, `2553ec8`, `f516ad0`, `e3338bd`, `d93f09c`, `4d280c6`, `633de1f`
- **فایل‌ها:** `scripts/`, `composer.json`
- **شرح:** اسکریپت استقرار روی هاست اشتراکی؛ تشخیص نسخه‌ی PHP، افزونه‌های گمشده، خطای اتصال دیتابیس، مقادیر `.env` که لاراول متفاوت می‌خواند، مالکیت پوشه‌ی storage، و فرمان `admin:create`.
- **وضعیت:** پایدار.
- **تست:** ندارد (اسکریپت شل).
- **ریسک رگرسیون:** پایین — خارج از مسیر اجرای اپ.

## ۲) بازنویسی فرانت به React SPA

- **کامیت‌ها:** `c230ac2`, `9a4601d`, `8a6d1ad`, `3166d81`, `e30e987`, `8b6ea9e`
- **فایل‌ها:** `resources/js/**`, `vite.config.js`
- **شرح:** حذف کامل پنل Blade و بازسازی همه‌ی صفحات مدیریتی با React؛ سیستم طراحی توکن‌محور؛ تقسیم باندل بر اساس مسیر.
- **وضعیت:** پایدار و کامل (۷۳ صفحه).
- **تست:** `SpaShellTest`, `SmokeRenderTest` (سمت سرور). **صفر تست سمت کلاینت.**
- **ریسک رگرسیون:** **متوسط‌روبه‌بالا** — بزرگ‌ترین سطح کد پروژه بدون تست فرانت.

## ۳) ابزارهای داشبورد

- **کامیت‌ها:** `45c14c3`, `6cbe8b2`, `6075868`, `97ebdf6`, `97e3858`, `6440911`
- **فایل‌ها:** `pages/search`, `pages/calculator`, `pages/profile`, `pages/account`, `components/layout/*`
- **شرح:** جست‌وجوی سراسری، اعلان‌ها، ماشین‌حساب (ضرب ضمنی، بستن خودکار پرانتز، deg/rad، مانیتور چسبان)، پروفایل با اعتبارسنجی سخت‌گیرانه، انتخابگر تاریخ شمسی، تولتیپ‌های سفارشی.
- **وضعیت:** پایدار.
- **تست:** `DashboardToolsTest`, `PolishTest`, واحد `ChargeCalculatorTest`.
- **ریسک رگرسیون:** پایین.

## ۴) سخت‌سازی امنیتی و عملیاتی ⚠️ حساس

- **کامیت‌ها:** `bd38390`, `a6c62b1`, `352c0a1`, `a4b5944`, `567736d`, `b4dcd86`, `36e6eb6`, `4d0d210`, `32dd884`, `8ac784f`
- **فایل‌ها:** `app/Models/Concerns/BelongsToComplex.php`, `app/Support/ComplexResolver.php`, `app/Http/Middleware/*`, `bootstrap/app.php`, `app/Services/Payment/*`
- **شرح:** بستن نشت بین‌مستأجری روی route-model binding؛ محدودیت نرخ روی احراز هویت؛ اعمال واقعی پلن‌ها؛ غیرفعال‌سازی درگاه سندباکس در پروداکشن؛ idempotent و مستقل‌از‌نشست کردن callback درگاه؛ مخفی‌سازی واقعی پیام؛ محافظت از بازیابی بکاپ؛ لاگ فعالیت (audit).
- **وضعیت:** پایدار و آزموده.
- **تست:** `TenantIsolationTest`, `CrossTenantReferenceTest`, `GatewayCallbackTest`, `SandboxGatewayTest`, `SystemRestoreTest`, `OperationalHardeningTest`, `MessageModerationTest`, `AuthThrottleTest`.
- **ریسک رگرسیون:** **بالا در صورت تغییر** — هر دست‌کاری در ترتیب میدل‌ورها یا `BelongsToComplex` می‌تواند ایزولاسیون مجتمع‌ها را بشکند. **قبل از تغییر، این تست‌ها را اجرا کن.**

## ۵) احراز هویت دومرحله‌ای و مدیریت نشست ⚠️ حساس

- **کامیت‌ها:** `8328d2e`, `1b0bbc5`, `f2b5763`, `007ec02`, `3780e7c`
- **فایل‌ها:** `app/Http/Controllers/Api/AuthController.php`, `app/Services/Auth/{OtpService,TrustedDeviceService}.php`, `app/Http/Middleware/AuthenticateTrustedDevice.php`, `resources/js/pages/auth/**`
- **شرح:** ورود رمز → OTP؛ دستگاه مورداعتماد ۱۰ روزه با توکن هش‌شده در کوکی امضاشده (خارج از `EncryptCookies`)؛ بازیابی رمز؛ پازل کپچا؛ قدرت رمز؛ ثبت‌نام **دومرحله‌ای** که حساب را فقط پس از تایید کد می‌سازد؛ پاک‌سازی OTP پس از ورود کامل.
- **وضعیت:** پایدار.
- **تست:** `TwoFactorLoginTest` (شامل ثبت‌نام دومرحله‌ای، ثبت‌نام رهاشده، ریست تایمر، دستگاه مورداعتماد)، `PhoneLoginTest`, `AuthThrottleTest`.
- **ریسک رگرسیون:** **بالا** — مسیر ورود کل سامانه.

## ۶) صفحه‌ی فرود و صفحات عمومی

- **کامیت‌ها:** `08f42a5`, `758a100`, `a824914`, `31bb173`, `3052ea5`, `ca6bac8`, `cc6fa25`
- **فایل‌ها:** `resources/js/pages/{home,demo,support}/**`, `public/images/*`
- **شرح:** تصاویر WebP، اسکرول نرم، لایت‌باکس گالری، صفحات دمو و پشتیبانی، چت پشتیبانی شناور، شخصیت CTA، تابلوی چراغانی دمو، بنرهای تبلیغاتی، شکست‌های نقطه‌ای هدر و برگر انیمیشنی.
- **وضعیت:** پایدار، ولی **۷ باگ UI باز** دارد (R3–R9 در `REMAINING_WORK.md`).
- **تست:** `SmokeRenderTest`, `AdvertisementTest`, `SupportChatTest`.
- **ریسک رگرسیون:** پایین (ارائه‌ای)، ولی بدون تست فرانت.

## ۷) MPA جزیره‌ای + SEO

- **کامیت‌ها:** `313395c`, `02320a4`, `27150a8`
- **فایل‌ها:** `routes/web.php`, `resources/views/layouts/public.blade.php`, `resources/js/entries/*.tsx`, `resources/js/lib/mpaNav.ts`, `public/{robots.txt,sitemap.xml,llms.txt}`
- **شرح:** صفحات عمومی به اسناد مستقل با `<head>` سرورساید تبدیل شدند؛ OG/Twitter/canonical؛ JSON-LD؛ robots/sitemap/llms.txt.
- **وضعیت:** پایدار.
- **تست:** `SpaShellTest`, `SmokeRenderTest`.
- **ریسک رگرسیون:** **متوسط** — مرز MPA/SPA ظریف است؛ ناوبری بین این دو باید بارگذاری کامل باشد (منشأ باگ ۴۰۳ که در `3b7987e` رفع شد).

## ۸) زیرساخت مدرن فرانت

- **کامیت‌ها:** `26a046d` (axios), `8a56cb2` (React Compiler), `f7f1e1a` (zustand), `e067b1c` + `0e17a8e` (UI خوش‌بینانه)
- **فایل‌ها:** `resources/js/lib/api.ts`, `vite.config.js`, `resources/js/context/*.tsx`
- **شرح:** یکسان‌سازی درخواست‌ها روی axios با مدیریت CSRF و بازتلاش ۴۱۹؛ فعال‌سازی React Compiler برای memoization خودکار؛ مهاجرت ۵ store از Context به zustand (**Redux عمداً نصب نشد**)؛ به‌روزرسانی خوش‌بینانه با بازگردانی.
- **وضعیت:** پایدار.
- **تست:** ندارد (سمت کلاینت).
- **ریسک رگرسیون:** متوسط — بدون تست فرانت، شکستِ خاموش ممکن است.

## ۹) پیامک و OTP (IPPanel)

- **کامیت‌ها:** `16441f0`, `53e3ce0`, `3b7987e`, `11e54a9`, `cc6fa25`
- **فایل‌ها:** `app/Services/Sms/{IppanelSms,SmsManager,SendsOtpAsMessage}.php`, `app/Http/Controllers/Api/System/SmsController.php`
- **شرح:** بازنویسی برای **Edge API** (`edge.ippanel.com/v1/api/send`) با `sending_type=pattern`، گیرنده در قالب E.164، و دفترچه تلفن اختیاری؛ پرشدن خودکار کد روی موبایل (WebOTP + `one-time-code`)؛ حذف پیامک یادآوری شارژ (**پیامک فقط برای OTP**)؛ ثبت کامل خطای ارسال در لاگ و نمایش آن در «ارسال آزمایشی»؛ کلید «ورود بدون OTP».
- **وضعیت:** کد پایدار؛ **ارسال واقعی با اعتبارنامه‌ی زنده آزموده نشده**.
- **تست:** `IppanelSmsTest` (۵ تست: پترن، E.164، دفترچه تلفن، fallback، `meta.status=false`).
- **ریسک رگرسیون:** متوسط — وابسته به سرویس بیرونی.

## ۱۰) اشتراک و پکیج‌های پویا ⚠️ حساس (مالی)

- **کامیت‌ها:** `bd38390`, `f575ab9`
- **فایل‌ها:** `app/Models/Plan.php`, `app/Contracts/PlanCapabilities.php`, `app/Services/Subscription/PlanGate.php`, `app/Http/Controllers/Api/System/PlanController.php`, مهاجرت‌های `2026_01_07_*`
- **شرح:** انتقال پلن‌ها از enum هاردکد به جدول `plans` که ادمین کل مدیریت می‌کند (ساخت/ویرایش/حذف، فعال‌سازی قابلیت‌ها) + فعال‌سازی/غیرفعال‌سازی دستی برای هر مجتمع. به‌صورت **افزایشی**: `plan_id` اختیاری روی `subscriptions` و enum قدیمی به‌عنوان fallback.
- **وضعیت:** پایدار؛ یک بدهی فنی شناخته‌شده (ستون سایه‌ی `plan` — بند B4 گزارش ممیزی).
- **تست:** `SubscriptionFlowTest`, `MaintenanceAndLimitsTest`, `DashboardToolsTest`.
- **ریسک رگرسیون:** **بالا** — مسیر پول. هر تغییر باید با کل `SubscriptionFlowTest` سنجیده شود.

## ۱۱) پنل ادمین کل

- **کامیت‌ها:** `527953e` (تبلیغات), `46f1fcd` (فوتر و شبکه‌ها), `ddcb64a` + `3780e7c` (اعضای سامانه), `f575ab9` (پکیج‌ها)
- **فایل‌ها:** `app/Http/Controllers/Api/System/*`, `resources/js/pages/system/*`
- **شرح:** مدیریت تبلیغات صفحه‌ی فرود؛ محتوای فوتر و لینک ۵ شبکه‌ی اجتماعی (شامل «بله»)؛ فهرست همه‌ی اعضا با جست‌وجو/تغییر نقش/حذف (شامل کاربران بدون مجتمع)؛ پکیج‌های اشتراک.
- **وضعیت:** پایدار.
- **تست:** `AdvertisementTest`; اعضا و پکیج‌ها **فاقد تست اختصاصی** (فقط دستی راستی‌آزمایی شدند).
- **ریسک رگرسیون:** **متوسط** — `MemberController` می‌تواند نقش‌ها را تغییر دهد و تست خودکار ندارد.

## ۱۲) هویت بصری

- **کامیت‌ها:** `3b7987e`, `3780e7c`
- **فایل‌ها:** `public/logo.webp`, `public/icons/*`, `resources/js/components/common/{LogoMark,SocialIcons}.tsx`
- **شرح:** لوگوی جدید برند در همه‌ی صفحات + فاویکون و آیکون‌های PWA؛ آیکون واقعی «بله» (قطره با تیک برش‌خورده) و «روبیکا» (شش‌ضلعی با مکعب).
- **وضعیت:** پایدار.
- **تست:** ندارد.
- **ریسک رگرسیون:** پایین.

---

## جمع‌بندی پوشش تست

| دامنه | تست موجود | ارزیابی |
|-------|-----------|---------|
| ایزولاسیون چندمستأجری | ۲ فایل اختصاصی | ✅ خوب |
| احراز هویت و OTP | ۳ فایل | ✅ خوب |
| مالی و اشتراک | ۴ فایل | ✅ خوب |
| درگاه پرداخت | ۲ فایل | ✅ خوب |
| عملیات و نگه‌داری | ۳ فایل | ✅ خوب |
| پنل ادمین کل (اعضا/پکیج‌ها) | — | ⚠️ شکاف |
| کل فرانت‌اند | — | ❌ صفر |
