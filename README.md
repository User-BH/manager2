# سامانه مدیریت ساختمان و مجتمع مسکونی

وب‌اپلیکیشن کامل مدیریت شارژ، هزینه، درآمد، پرداخت آنلاین، پیام‌رسان، اطلاعیه و گزارش‌های مالی برای مجتمع‌های مسکونی، با پشتیبانی از **چند مجتمع (multi-tenant)**، رابط **فارسی و راست‌چین**، تاریخ **شمسی**، و عدم وابستگی به سرویس‌های خارجی (مناسب شرایط ایران).

> این سند هم نقش «تحلیل و طراحی» (خروجی‌های ۱ تا ۱۴) و هم «راهنمای اجرا» را دارد. بخش زیادی از سیستم پیاده‌سازی و تست شده است؛ نقشهٔ راه ادامه در انتهای سند آمده.

---

## ۱) خلاصهٔ تحلیل نیازمندی‌ها

- چهار نقش: **ادمین کل سیستم**، **مدیر مجتمع**، **مالک**، **مستاجر**.
- جداسازی کامل دادهٔ مجتمع‌ها از یکدیگر (هر کاربر فقط دادهٔ مجتمع خود را می‌بیند).
- موتور انعطاف‌پذیر محاسبهٔ شارژ با ۸ روش و قابلیت توسعه.
- تفکیک هزینه‌های **مالکانه** و **مستاجرانه** در سطح هر قلم هزینه و هر قبض.
- پرداخت **آنلاین** (درگاه) و **آپلود رسید** با گردش‌کار تایید مدیر.
- داشبورد مالی، نمودار روند، بدهکاران، خوش‌حساب‌ها.
- پیام‌رسان متنی داخلی با مدیریت (بستن کلی، محدودیت کاربر، مخفی‌سازی پیام).
- اطلاعیه با مخاطب‌گذاری و سنجاق.
- بکاپ در دو سطح (کل سیستم / هر مجتمع).
- امنیت: RBAC، ایزولاسیون سطح-کوئری، اعتبارسنجی، حفاظت فایل آپلود، لاگ فعالیت.

## ۲) پشتهٔ فنی (Technology)

| لایه | انتخاب | چرایی |
|------|--------|-------|
| بک‌اند | **Laravel 13 (PHP 8.4)** | ORM + Migration، RBAC، اکوسیستم فارسی غنی، مناسب CRUD مالی |
| دیتابیس | **MySQL / MariaDB** (پروداکشن) — مدیریت با **phpMyAdmin** | طبق نیاز پروژه؛ Migrationها قابل‌حمل و روی SQLite هم اجرا می‌شوند |
| فرانت | **Blade + Livewire + Tailwind v4 + Alpine** | سرور-رندر، سبک، RTL آسان، تعامل بدون SPA سنگین |
| تاریخ شمسی | **morilog/jalali** | تبدیل میلادی↔جلالی، نمایش شمسی در کل UI |
| فونت | **Vazirmatn (Self-Hosted)** | بدون CDN خارجی؛ فایل‌های `woff2` داخل پروژه |
| نمودار | **Chart.js (bundled)** | self-hosted از طریق Vite |
| پرداخت | درایور قابل‌تعویض (`PaymentGateway`) | سندباکس آماده؛ جای اتصال ملت/سامان مشخص |

نکتهٔ ایران: تمام CSS/JS/فونت‌ها با Vite باندل و **self-host** می‌شوند؛ هیچ منبع خارجی برای نمایش UI لازم نیست.

## ۳) معماری کلی

```
app/
  Enums/                 انواع نوع‌بندی‌شده (نقش، نوع شارژ، وضعیت‌ها) با برچسب فارسی
  Models/                مدل‌ها + Concerns/BelongsToComplex + Scopes/ComplexScope
  Http/
    Controllers/         وب (Admin/*, System/*, ساکن)
    Middleware/          SetCurrentComplex, EnsureRole, EnsureActive
  Livewire/              Messenger (پیام‌رسان)
  Services/
    Charge/              ChargeCalculator, BillGenerator, PenaltyCalculator, ChargeComponent
    Payment/             PaymentService, GatewayManager, PaymentGateway, FakeGateway
    ReportService        محاسبات داشبورد و گزارش‌ها
  Support/               Jalali (تاریخ/عدد/پول فارسی)، TenantContext
resources/views/         layouts, components (card/stat/badge/input/...), صفحات
database/migrations/     ساختار جداول
database/seeders/        DemoSeeder (دادهٔ نمونهٔ کامل)
```

### چند-مستأجری (Multi-Tenancy)
- هر جدول داده، ستون `complex_id` دارد.
- `TenantContext` (singleton) شناسهٔ مجتمع فعالِ درخواست را نگه می‌دارد؛ میدل‌ور `SetCurrentComplex` آن را از کاربرِ واردشده تنظیم می‌کند.
- `ComplexScope` (Global Scope) روی همهٔ مدل‌هایی که از تریت `BelongsToComplex` استفاده می‌کنند، خودکار `where complex_id = ?` می‌زند و هنگام ساخت رکورد، `complex_id` را پر می‌کند. → **نشت دادهٔ بین‌مجتمعی در سطح کوئری ناممکن است**، نه صرفاً با یادآوری در کنترلر.
- ادمین کل: بدون مجتمع فعال، نمای سیستمی؛ با «انتخاب مجتمع» وارد اسکوپ آن می‌شود.

## ۴) طراحی دیتابیس

جداول اصلی: `complexes`, `buildings`, `units`, `unit_user` (پیوت مالک/مستاجر با سابقهٔ جابه‌جایی), `users`, `charge_rules`, `expenses`, `incomes`, `bills`, `payments`, `announcements`, `messages`, `message_restrictions`, `audit_logs`, `backups`, `settings`.

نکات کلیدی:
- `unit_user` با `is_current` و `end_date` سابقهٔ مالک/مستاجر قبلی را حفظ می‌کند (جابه‌جایی مستاجر) و `share_percent` برای چند-مالکی.
- `bills` ریز محاسبه را در ستون JSON `breakdown` نگه می‌دارد و سهم `owner_amount`/`tenant_amount` را تفکیک می‌کند.
- `payments` هم پرداخت آنلاین (gateway/ref/tracking) و هم رسید (receipt_path/review) را پوشش می‌دهد.
- درگاه پرداخت در `complexes.gateway_config` (JSON) نگهداری می‌شود (به‌جای جدول مجزا) و قابل توسعه است.

> توجه: `period` دوره‌ها به‌صورت رشتهٔ شمسی `YYYY-MM` (مثل `1404-03`) ذخیره می‌شود؛ تاریخ‌های واقعی (سررسید، پرداخت) به میلادی در DB و شمسی در UI.

## ۵) نقش‌ها و سطح دسترسی

| نقش | دسترسی |
|-----|--------|
| `super_admin` | کل سیستم، ساخت/انتخاب مجتمع، نمای کلی |
| `complex_admin` | مدیریت کامل مجتمع خود: واحدها، ساکنین، قوانین شارژ، هزینه/درآمد، صدور قبض، تایید پرداخت، اطلاعیه، تنظیمات، بکاپ |
| `owner` | پروفایل، واحدها، قبوض، پرداخت، اطلاعیه، پیام‌رسان |
| `tenant` | مشابه مالک با تمرکز بر شارژ مستاجرانه |

اعمال با میدل‌ور `role:` و ایزولاسیون با `ComplexScope`.

## ۶ تا ۸) صفحات، داشبورد و منطق شارژ

- **داشبورد مدیر:** درآمد/هزینهٔ ماه، مانده صندوق، بدهی کل، نمودار ۶ ماهه، وضعیت پرداخت، بدهکاران، خوش‌حساب‌ها.
- **داشبورد ساکن:** واحدها، بدهی، آخرین قبض، اطلاعیه‌ها.
- **موتور شارژ (`ChargeCalculator`):** ۸ روش —
  `Fixed`, `PerPerson`, `PerArea`, `Combined`, `UtilityByPerson`, `ByUnitCount`, `ByCoefficient`, `ElevatorByFloor`.
  - روش‌های «استخری» یک مبلغ کل را با **روش بزرگ‌ترین باقی‌مانده** بین واحدها تقسیم می‌کنند تا جمع سهم‌ها دقیقاً برابر مبلغ کل شود (بدون گم‌شدن ریال).
  - هر قلم به دستهٔ مالکانه/مستاجرانه می‌رود؛ آسانسور می‌تواند همکف را معاف کند.
- **`BillGenerator`:** قوانین پایه + هزینه‌های قابل‌تقسیم دوره را به قبض هر واحد تبدیل می‌کند، جریمهٔ دیرکرد قبوض معوق و تخفیف را اعمال می‌کند، و idempotent است (قبض تسویه‌شده را بازنویسی نمی‌کند).
- **`PenaltyCalculator`:** جریمهٔ ثابت / درصدی / درصد روزانه با روزهای مهلت.

## ۹) پرداخت

- **آنلاین:** درایور `PaymentGateway` با گردش دو مرحله‌ای `request`→`verify`. `FakeGateway` کل مسیر را بدون شبکهٔ خارجی شبیه‌سازی می‌کند؛ جای `MellatGateway`/`SamanGateway` در `GatewayManager` مشخص است.
- **رسید:** آپلود فایل (jpg/png/pdf، حداکثر ۴MB، ذخیره روی دیسک خصوصی)، وضعیت «در انتظار تایید»، تایید/رد توسط مدیر، تخصیص خودکار به قبوض (قدیمی‌ترین اول) و به‌روزرسانی مانده.

## ۱۰–۱۲) پیام‌رسان، اطلاعیه، بکاپ

- **پیام‌رسان (Livewire):** فقط متن؛ نمایش نام/نقش/واحد/زمان شمسی؛ بستن کلی مجتمع، محدودیت کاربر، مخفی‌سازی پیام توسط مدیر.
- **اطلاعیه:** عنوان/متن/مخاطب (همه/مالکین/مستاجرین)/سنجاق/فعال، نمایش در داشبورد.
- **بکاپ مجتمع:** خروجی JSON خودکفا از واحدها، کاربران، مالی، قبوض و پرداخت‌های همان مجتمع (داده‌های سایر مجتمع‌ها نمی‌آید). بکاپ کل سیستم در نقشهٔ راه.

## ۱۳) امنیت

- احراز هویت Laravel، رمز عبور **bcrypt**.
- RBAC با میدل‌ور نقش + ایزولاسیون سطح-کوئری (`ComplexScope`).
- جلوگیری از SQLi (Eloquent/Query Builder)، XSS (Blade escaping)، CSRF (توکن در همهٔ فرم‌ها).
- اعتبارسنجی کامل سمت سرور + پیام‌های خطای فارسی.
- محدودیت نوع/حجم فایل رسید؛ ذخیره روی دیسک **خصوصی** و سرویس‌دهی کنترل‌شده.
- غیرفعال‌سازی کاربر (`is_active`) با خروج فوری؛ لاگ فعالیت (`audit_logs`).

## ۱۴) UI

ساده، مدرن، مینیمال، ریسپانسیو، RTL، **حالت روشن/تاریک** هماهنگ با تنظیم سیستم (با امکان تغییر دستی و ذخیره)، اعداد و تاریخ فارسی، فونت Vazirmatn.

---

## راه‌اندازی

### پیش‌نیاز
PHP 8.3+، Composer، Node 18+/npm. برای پروداکشن: MySQL/MariaDB.

### اجرای سریع (محیط توسعه با SQLite)
```bash
composer install
npm install && npm run build
cp .env.example .env          # برای SQLite مقدار DB_CONNECTION=sqlite را فعال کنید
php artisan key:generate
touch database/database.sqlite
php artisan migrate --seed
php artisan serve
```

### پروداکشن با MySQL
1. در `.env` مقادیر `DB_CONNECTION=mysql` و `DB_DATABASE/USERNAME/PASSWORD` را تنظیم کنید (دیتابیس را در phpMyAdmin بسازید).
2. `php artisan migrate --seed` و سپس `npm run build`.

### حساب‌های نمونه (رمز همه: `password`)
| نقش | ایمیل |
|-----|-------|
| ادمین کل | `admin@system.test` |
| مدیر مجتمع | `manager@aftab.test` |
| مالک | `owner1@aftab.test` |
| مستاجر | `tenant2@aftab.test` |

### تست
```bash
php artisan test
```
شامل تست واحد موتور شارژ (۱۰ سناریو) و تست یکپارچگی صدور قبض/تسویه.

---

## نقشهٔ راه (پیاده‌سازی ادامه)
موارد طراحی‌شده که برای تکمیل باقی مانده‌اند: خروجی PDF/Excel گزارش‌ها و فاکتور شارژ، بکاپ زمان‌بندی‌شدهٔ کل سیستم + بازیابی، یادآوری پیامکی پرداخت، PWA، اتصال واقعی درگاه‌های ملت/سامان، صفحهٔ تخفیف/بخشودگی اختصاصی، و گزارش بدهی هنگام تخلیه/فروش. ساختار سرویس‌ها و دیتابیس برای افزودن این موارد آماده است.
