{{--
    وضعیتِ بازدیدکننده برای صفحاتِ عمومی (R18).

    ─── چرا سرورساید و نه یک درخواستِ API؟ ────────────────────────────────
    صفحاتِ عمومی جزیره‌ی MPA‌اند و نشستِ کاربر همین حالا سمتِ سرور معلوم است.
    اگر فرانت می‌خواست با `/api/me` بپرسد، کاربرِ واردشده اول دکمه‌ی «ورود» را
    می‌دید و یک لحظه بعد به «ورود به داشبورد» تغییر می‌کرد — همان پرشی که
    اسکریپتِ تم بالاتر برای رنگ‌ها جلویش را می‌گیرد.

    ─── هیچ داده‌ی شخصی‌ای اینجا نمی‌آید ──────────────────────────────────
    فقط یک بولین. نه نام، نه شماره، نه نقش. دلیلش کش است: اگر روزی CDN جلوی
    صفحه‌ی فرود بنشیند، بدترین حالت این است که مهمانی دکمه‌ی «ورود به داشبورد»
    ببیند و با کلیک به صفحه‌ی ورود برود — نه نشتِ اطلاعات.

    تگ فقط برای کاربرِ واردشده چاپ می‌شود، پس پاسخِ مهمان بایت‌به‌بایت همان
    چیزی است که قبلاً بود و کش‌پذیریِ آن دست‌نخورده می‌ماند.
--}}
@auth
    <script type="application/json" id="viewer-state">
        {!! \App\Support\Json::forScript(['authenticated' => true]) !!}
    </script>
@endauth
