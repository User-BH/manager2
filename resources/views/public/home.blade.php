{{-- صفحه‌ی خانه (island). متادیتا و JSON-LDِ سازمان/محصول از layout می‌آید. --}}
@extends('layouts.public')

{{--
    نشانِ اعتمادِ الکترونیکی (اینماد).

    ─── چرا در Blade و نه در کامپوننتِ React ─────────────────────────────────
    بررسیِ خودکارِ اینماد صفحه را **بدونِ اجرای جاوااسکریپت** می‌خواند. کلِ
    صفحه‌ی فرود یک island است و فوتر سمتِ کلاینت ساخته می‌شود، پس نشانی که
    آنجا رندر شود در HTMLِ خامِ سرور اصلاً وجود ندارد.

    ─── چرا `display:none` ───────────────────────────────────────────────────
    تا پیش از mountِ React، `#root` خالی است؛ یعنی این گره تنها چیزِ قابلِ
    دیدنِ صفحه می‌شد و **بالای صفحه** ظاهر می‌شد، بعد با آمدنِ فوتر به پایین
    می‌پرید. `TrustSeal` پس از جابه‌جا کردنش نمایانش می‌کند، پس نه پرشی هست
    و نه چشمک.

    این برای کاربرِ بدونِ جاوااسکریپت چیزی را از دست نمی‌دهد: بدونِ JS اصلاً
    هیچ بخشی از این صفحه رندر نمی‌شود.

    ─── چرا تصویر محلی است ───────────────────────────────────────────────────
    پیش از این از `trustseal.enamad.ir` خوانده می‌شد و یک رفت‌وبرگشتِ
    شخصِ‌ثالث به مسیرِ بحرانیِ صفحه‌ی فرود اضافه می‌کرد. همان فایل (۱٫۹
    کیلوبایت) محلی سرو می‌شود.

    ابعادِ ۹۴×۱۰۲ نسبتِ واقعیِ تصویر (۱۲۵×۱۳۶) را نگه می‌دارد؛ با ۹۴×۹۴
    مرورگر جای اشتباه رزرو می‌کرد و فوتر هنگامِ رسیدنِ تصویر ۸ پیکسل
    می‌پرید.

    ⚠️ `href` و ویژگیِ `code` **دقیقاً** همان چیزی‌اند که اینماد می‌دهد و
    نباید تغییر کنند؛ تاییدیه به همان‌ها بند است.
--}}
@if (config('brand.enamad.id'))
    @push('body_end')
        <div id="enamad-seal" style="display:none">
            <a referrerpolicy="origin" target="_blank" rel="noopener"
               href="https://trustseal.enamad.ir/?id={{ config('brand.enamad.id') }}&Code={{ config('brand.enamad.code') }}">
                <img referrerpolicy="origin" style="cursor:pointer" width="94" height="102"
                     alt="نماد اعتماد الکترونیکی" src="/images/enamad.png"
                     code="{{ config('brand.enamad.code') }}">
            </a>
        </div>
    @endpush
@endif
